# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,seu-dominio.com.br
DJANGO_LANGUAGE_CODE=pt-br
DJANGO_TIME_ZONE=America/Sao_Paulo

# Security Settings (apenas em produção com HTTPS)
DJANGO_SESSION_COOKIE_SECURE=True
DJANGO_SECURE_SSL_REDIRECT=True

# Database PostgreSQL
POSTGRES_DB=sistema_carros
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Para plataformas de deploy (Railway, Render, etc.), as variáveis acima
# geralmente são configuradas automaticamente. Verifique a documentação
# da sua plataforma para os nomes exatos das variáveis de ambiente.

